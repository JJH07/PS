#include <bits/stdc++.h>

int main()
{
    std::ios::sync_with_stdio(false);
    std::cin.tie(NULL);
    
    int n;
    std::cin >> n;
    for(int i = 0; i < n / 4; i++)
    {
        std::cout << "long ";
    }
    std::cout << "int";

    return 0;
}
