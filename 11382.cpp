#include <bits/stdc++.h>

int main()
{
    std::ios::sync_with_stdio(false);
    std::cin.tie(NULL);

    unsigned long long a, b, c, d;
    std::cin >> a >> b >> c;
    d = a + b + c;
    std::cout << d;

    return 0;
}
