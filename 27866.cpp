#include <bits/stdc++.h>

int main()
{
    std::ios::sync_with_stdio(false);
    std::cin.tie(NULL);

    char word[1001];
    int n;
    std::cin >> word >> n;
    std::cout << word[n-1];
    return 0;
}
